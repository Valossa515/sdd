---
description: Turn a feature spec into an architecture contract (.contract.toon) (SDD architect role)
argument-hint: <feature name or spec path>
---

Read `${CLAUDE_PLUGIN_ROOT}/agents/architect.md` and adopt the architect role for this task, following all of its constraints and output formats.

If the project has a `.agent/SKILLS.md`, read it first and follow the linked skills and project overrides. Read the referenced feature spec and acceptance criteria before designing, and write the contract to `.agent/specs/contracts/`.

Feature to architect: $ARGUMENTS
