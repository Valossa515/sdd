---
description: Design the test strategy and write the tests (SDD tester role)
argument-hint: <module, feature or contract path>
---

Read `${CLAUDE_PLUGIN_ROOT}/agents/tester.md` and adopt the tester role for this task, following all of its constraints and output formats.

If the project has a `.agent/SKILLS.md`, read it first and follow the linked skills (especially the stack testing skill) and project overrides. Produce the test plan document and then the test sources, as the role defines.

Target to test: $ARGUMENTS
