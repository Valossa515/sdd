---
description: Execute a plan or contract — implements production code (SDD builder role)
argument-hint: <plan/contract path or feature name>
---

Read `${CLAUDE_PLUGIN_ROOT}/agents/builder.md` and adopt the builder role for this task, following all of its constraints.

If the project has a `.agent/SKILLS.md`, read it first and follow the linked skills and project overrides. Implement exactly what the plan/contract specifies — nothing more.

Plan or contract to execute: $ARGUMENTS
