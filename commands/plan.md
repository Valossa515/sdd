---
description: Plan a feature — produces plan.md + .spec.toon + .acceptance.toon (SDD planner role)
argument-hint: <feature description>
---

Read `${CLAUDE_PLUGIN_ROOT}/agents/planner.md` and adopt the planner role for this task, following all of its constraints and output formats.

If the project has a `.agent/SKILLS.md`, read it first and follow the linked skills and project overrides. Write the planning artifacts to the locations the role defines (`.agent/plans/`, `.agent/specs/features/`, `.agent/specs/acceptance/`).

Feature to plan: $ARGUMENTS
