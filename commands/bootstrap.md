---
description: Create or update the project's .agent/ context docs (SDD bootstrap skill)
argument-hint: [optional focus, e.g. "only glossary"]
---

Read the `sdd:bootstrap` skill (`${CLAUDE_PLUGIN_ROOT}/skills/shared/bootstrap/SKILL.md`) and follow it for this task.

Map the repository and create/update the `.agent/` context files (context.md, architecture.md, decisions.md, conventions.md, runbook.md, glossary.md, backlog_rules.md). Do not invent facts — add TODOs for unknowns.

Focus: $ARGUMENTS
