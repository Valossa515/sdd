# Project Reference

> **Auto-generated by `@bootstrap`.** Single-file reference consolidating project overview, architecture, class map, conventions, and domain glossary. Use this as the go-to document to understand the project.

---

## 1. Project Overview

| Field | Value |
|-------|-------|
| **Name** | [project name] |
| **Description** | [one-line description from README or build file] |
| **Type** | [API / Web App / CLI / Library / Microservice] |
| **Stack** | [language + framework + major libs] |
| **Build tool** | [Maven / Gradle / dotnet / npm / etc.] |
| **Runtime version** | [Java 21 / .NET 8 / Node 20 / etc.] |
| **Source root** | [src/main/java/... or src/ or lib/] |
| **Test root** | [src/test/java/... or tests/ or test/] |

### Problem & Purpose
- [What problem this system solves — from README or TODO if unknowable from code]

### Key Flows
1. [Main flow A — discovered from controllers/routes]
2. [Main flow B]

---

## 2. Architecture

- **Style:** [Layered / Hexagonal / Clean Architecture / MVC]
- **Packaging:** [by layer / by feature / hybrid]
- **Base package:** [e.g., `com.example.orders`]

### Layer Map

| Layer | Package / Folder | Responsibility |
|-------|-----------------|----------------|
| API | `controller/` | HTTP entry points, request validation |
| Application | `service/` | Business orchestration, transactions |
| Domain | `model/` | Entities, value objects, domain rules |
| Infrastructure | `repository/`, `config/`, `client/` | Persistence, external clients, config |

### External Integrations

| System | Protocol | Purpose |
|--------|----------|---------|
| [Database] | [JDBC/ORM] | Primary data store |
| [External API] | [HTTP] | [purpose] |

---

## 3. Module & Class Map

> The agent MUST populate this section by scanning every package/namespace in the source tree. Remove categories that don't apply to the project.

### Controllers / Entry Points

| Class | Path | Responsibility | Endpoints |
|-------|------|----------------|-----------|
| `OrderController` | `src/.../controller/OrderController.java` | Order CRUD operations | `POST /api/v1/orders`, `GET /api/v1/orders/{id}` |

### Services / Use Cases

| Class | Path | Responsibility | Key Dependencies |
|-------|------|----------------|-----------------|
| `OrderService` | `src/.../service/OrderService.java` | Order creation, validation, stock checks | `OrderRepository`, `StockClient` |

### Entities / Domain Models

| Class | Path | Responsibility | Key Fields |
|-------|------|----------------|-----------|
| `Order` | `src/.../model/Order.java` | Customer order with items and status | `id`, `customerId`, `items`, `status`, `createdAt` |

### Repositories / Data Access

| Class | Path | Responsibility | Custom Queries |
|-------|------|----------------|---------------|
| `OrderRepository` | `src/.../repository/OrderRepository.java` | Order persistence | `findByCustomerId`, `findByStatus` |

### DTOs / Request-Response Objects

| Class | Path | Used By | Direction |
|-------|------|---------|-----------|
| `CreateOrderRequest` | `src/.../dto/CreateOrderRequest.java` | `OrderController.create` | Input |
| `OrderResponse` | `src/.../dto/OrderResponse.java` | `OrderController.*` | Output |

### Configuration & Cross-cutting

| Class | Path | Responsibility |
|-------|------|----------------|
| `SecurityConfig` | `src/.../config/SecurityConfig.java` | Auth setup (JWT / Basic / etc.) |
| `GlobalExceptionHandler` | `src/.../config/GlobalExceptionHandler.java` | Centralized error handling |

### Mappers / Utilities

| Class | Path | Responsibility |
|-------|------|----------------|
| `OrderMapper` | `src/.../mapper/OrderMapper.java` | Entity ↔ DTO conversion |

> **Note:** Replace all examples above with the actual classes found in the project.

---

## 4. Available Endpoints

<!-- Auto-populated from controller/route scan -->

| Method | Path | Controller | Description | Auth |
|--------|------|------------|-------------|------|
| POST | `/api/v1/orders` | `OrderController` | Create a new order | Yes |
| GET | `/api/v1/orders/{id}` | `OrderController` | Get order by ID | Yes |

---

## 5. Database Schema

<!-- Auto-populated from entities and migrations -->

### Tables

| Table | Entity | Key Columns | Notes |
|-------|--------|-------------|-------|
| `orders` | `Order` | `id (PK)`, `customer_id`, `status`, `created_at` | Soft delete: no |

### Enums / Status Values

| Enum | Values | Used In |
|------|--------|---------|
| `OrderStatus` | `PENDING`, `CONFIRMED`, `CANCELLED` | `orders.status` |

---

## 6. Conventions Summary

### Naming

| Element | Convention | Example |
|---------|-----------|---------|
| Classes | PascalCase | `OrderService` |
| Methods | camelCase | `createOrder` |
| Tables | snake_case | `customer_orders` |
| Endpoints | kebab-case | `/api/v1/customer-orders` |
| Constants | UPPER_SNAKE | `MAX_RETRY_COUNT` |

### Project Structure

```
src/
├── main/
│   ├── java/com/example/project/
│   │   ├── controller/
│   │   ├── service/
│   │   ├── repository/
│   │   ├── model/
│   │   ├── dto/
│   │   └── config/
│   └── resources/
│       ├── application.yml
│       └── db/migration/
└── test/
```

> **Note:** Replace with actual project tree.

### Key Technical Choices

| Concern | Choice |
|---------|--------|
| ORM / Data access | [JPA / MyBatis / Dapper / EF Core] |
| Migrations | [Flyway / Liquibase / EF Migrations] |
| DI style | [Constructor injection] |
| Error handling | [Global exception handler / middleware] |
| Auth | [JWT / Basic / API Key / none] |
| Logging | [SLF4J / Serilog / console] |
| Test framework | [JUnit 5 / xUnit / Jest] |
| Mock library | [Mockito / Moq / NSubstitute] |
| Integration tests | [TestContainers / H2 / SQLite] |

---

## 7. Domain Glossary

### Entities & Concepts

| Term | Definition | Source |
|------|-----------|--------|
| **Order** | A customer purchase containing one or more items | `Order.java` |

### Status Codes & Enums

| Enum | Values | Meaning |
|------|--------|---------|
| `OrderStatus` | PENDING, CONFIRMED, CANCELLED | Lifecycle states of an order |

### Disambiguation

| Term A | Term B | Difference |
|--------|--------|-----------|
| Order | Request | Order is persisted; Request is the input DTO |

---

## 8. Dependencies

<!-- Auto-populated from build file -->

| Dependency | Version | Purpose |
|------------|---------|---------|
| spring-boot-starter-web | 3.x | REST API framework |
| spring-boot-starter-data-jpa | 3.x | ORM / data access |
| flyway-core | 10.x | Database migrations |
| ... | ... | ... |

---

## 9. How to Run

```bash
# Build
[build command]

# Run
[run command]

# Test
[test command]

# Migrations
[migration command]
```

---

*Last updated: [date of bootstrap execution]*
